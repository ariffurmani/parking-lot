package service;

public class FindSlotStrategyImpl implements FindSlotStrategy {

    @Override
    public String findSlot(String vehicleType) {
        return "";
    }

    @Override
    public boolean isSlotAvailable(String vehicleType) {
        return false;
    }
}
